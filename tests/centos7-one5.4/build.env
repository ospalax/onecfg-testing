# base system
DOCKER_BASE=centos
DOCKER_TAG=centos7

# initial opennebula system (FROM which we do upgrade)
ONE_VERSION=5.4

# or instead use this repo for the initial version
#OPENNEBULA_REPO_URL='https://downloads.opennebula.org/repo/5.4/CentOS/7/$basearch'

# updated opennebula system (TO which we do upgrade)
#UPDATED_ONE_VERSION=5.4

# or instead an opennebula repo with the updated version
UPDATED_OPENNEBULA_REPO_URL='http://services/build/5.10.1-25d89c85-e5d984e5-3072/centos7/repo/CentOS/7/$basearch'

# onescape repo (as of now an explicit repo is mandatory - onescape is still WIP)
ONESCAPE_REPO_URL='http://services/build/onescape-111/centos7/repo/CentOS/7/$basearch/'
